// [application/selectors] 파생 상태 — UI 분기는 오직 이 selector로만(State Driven UI). (부록 B-2, C-4)
// 의존성 방향: Application → Domain. atoms를 읽어 파생값을 만든다. UI를 import하지 않는다.
// 다음 단계 예정(스펙 §3.5): isSajuMode, canSend, affinityLevel …
export {};
