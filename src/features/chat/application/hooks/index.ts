// [application/hooks] useChat() = Application Service — UI ↔ infrastructure orchestration. (스펙 §3.5)
// 의존성 방향: Application → Domain. atoms/selectors/commands를 묶어 UI에 노출한다. UI를 import하지 않는다.
// 다음 단계 예정: useChat() (UI는 이 hook과 selector 파생 상태로만 동작)
export {};
