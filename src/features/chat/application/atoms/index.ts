// [application/atoms] Jotai 전역 상태 — 상태 로직은 Application 레이어에만. (부록 B-3)
// 의존성 방향: Application → Domain (model/state import O). UI·Infrastructure를 import하지 않는다.
// 다음 단계 예정(스펙 §3.5): chatAtom, walletAtom, modeAtom …
export {};
